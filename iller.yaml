---
doctype: dist

header:
  name: MoopsX-UsingMoose

plugins:
 - +config: Author::CSSON

 - +add_plugin: TravisYML
   +before: Git::Tag
   perl_version: 5.14 5.20

---
doctype: weaver

plugins:
  - +config: Author::CSSON

  - +remove_plugin: Splint
